#!/bin/sh -e

EXEC=psql

if [ "$PG_DUMP" != "" ]; then
  EXEC=pg_dump
fi

PGPASSWORD=<%= @config[:password] %> $EXEC --username=<%= @config[:username] %> --host=<%= @config[:host] %> <%= @config[:database] %> "$@"