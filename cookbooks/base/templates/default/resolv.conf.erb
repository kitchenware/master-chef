<%
  config = node[:force_override_resolv_conf] || node[:override_resolv_conf] || node.resolv_conf
  config.keys.sort.each do |k|
    v = config[k]
    if v.is_a? Array
      v.sort.each do |vv|
%>
<%= k %> <%= vv %>
<%
      end
    else
%>
<%= k %> <%= v %>
<%
    end
  end
%>