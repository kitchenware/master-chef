
global
  <%= @config[:global][:log_config] %>
  maxconn <%= @config[:global][:maxconn] %>
  ulimit-n <%= @config[:global][:ulimit] %>
  user haproxy
  group haproxy
  daemon
  stats socket <%= @config[:socket] %> user haproxy level admin
  <% if @config[:global][:custom_params] %>
  <% @config[:global][:custom_params].each do |k, v| %>
  <%= k %> <%= v %>
  <% end %>
  <% end %>

