TIME_ZONE = '<%= @timezone %>'

DATABASES = {
   'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': '<%= @db_file %>',
        'USER': '',
        'PASSWORD': '',
        'HOST': '',
        'PORT': '',
    }
}

DATABASE_NAME = '<%= @db_file %>'