TIME_ZONE = '<%= @timezone %>'

DATABASES = {
   'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': '<%= @db_file %>',
        'USER': '',
        'PASSWORD': '',
        'HOST': '',
        'PORT': '',
    }
}

# Needed for old django version
DATABASE_ENGINE = 'django.db.backends.sqlite3'

DATABASE_NAME = '<%= @db_file %>'

SECRET_KEY = '<%= @secret_key %>'